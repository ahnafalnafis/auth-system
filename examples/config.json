{
  "database.type": "PostgreSQL",
  "database.configured": true,
  "database.config": {
    "PostgreSQL": {
      "tablename": "example_users",
      "connection.string": {
        "dbname": "example"
      }
    }
  }
}
